# Research sidecar - race-o-bot

Generated by `case-study-from-repo` on 2026-06-07 from `C:\Users\smith\Documents\2 Projects\aiderby`.

**Use this file as your fact-check reference when editing `race-o-bot.md`.** Every claim in the draft should be traceable back to a line here, or to one of the questions you answered during generation. If something can't be traced, treat it as a placeholder to verify before publishing.

---

## 1 · Repo identity

- **Path:** `C:\Users\smith\Documents\2 Projects\aiderby`
- **Slug derived:** `race-o-bot`
- **Default branch:** `master`
- **Remote (if set):** not inspected - local-only working copy assumed

## 2 · README excerpt

No root `README.md` exists. The canonical project description lives in two parallel guidance files:

- `CLAUDE.md` (for Claude Code)
- `AGENTS.md` (for Codex)

Both open with the same one-liner:

> Race-o-Bot is an autonomous prompt optimization system for horse racing predictions. It uses **retrodiction** (predicting past events with known outcomes) to iteratively improve LLM prompt templates, optimizing for trifecta ROI and top-3 accuracy against historical NA race data.
>
> Inspired by Karpathy's autoresearch pattern, adapted for prompt engineering instead of model training.

Sub-READMEs exist at `retrodiction-harness/README.md` and `rob-api/README.md` (not inspected here).

## 3 · Stack manifest

**Manifest file detected:** `requirements.txt` (root) - minimal, only `supabase>=2.0.0` and `pytest>=7.4.0`.

- **Language(s):** Python 3.10+ (primary); TypeScript/React surface in `rob-api/dashboard/` (Lovable-generated)
- **Frameworks:** Python stdlib only for the harness and predictions; Supabase Python SDK for persistence; Flask/FastAPI surface in `ai-derby/server.py` and `rob-api/src/` (not confirmed)
- **Key runtime deps:**
  - `supabase` (only declared pip dep - for race-day persistence)
  - Anthropic REST API via raw `urllib` (no SDK - see `evaluate.py:call_llm()`)
  - TheRacingAPI (HTTP)
  - ElevenLabs (voice agent, integration referenced in CLAUDE.md)
- **Key dev/build deps:** `pytest` only
- **Scripts of interest:**
  - `retrodiction-harness/scripts/normalize.py` - joins entries + results JSON
  - `retrodiction-harness/scripts/evaluate.py` - renders prompt, calls LLM, parses, scores
  - `retrodiction-harness/scripts/iterate.py` - autonomous keep/revert/mutate loop
  - `ai-derby/build_racecard.py` - pulls TheRacingAPI → Supabase
  - `ai-derby/predictions.py` - generates R.O.B. picks; **overwrites picks JSON every run**
  - `ai-derby/commentary.py` - pre/post-race narrative
  - `ai-derby/scripts/db.py` - single source of truth for Supabase writes (partial-update upserts)

## 4 · Architecture sketch

**Top-level folders:**

```
.claude/                 # Claude Code skills + settings
.codex/                  # Codex equivalent
ai-derby/                # Race-day operator stack (Don + Erica's surface)
aiderby-946dd8e5/        # Probable worktree/checkpoint copy
analysis/                # ROI rollups (one-pager pipeline)
archive/                 # 2026-derby-weekend artifacts (picks PDFs, one-off scripts)
docs/                    # Handoffs, runbooks, issue tracker
graphify-out/            # Knowledge graph (5,433 nodes / 7,952 edges)
plans/                   # Tracer-bullet plans
retrodiction-harness/    # Offline prompt-tuning lab (the "research" side)
rob-api/                 # Public API + Lovable dashboard surface
```

**Spine files inspected:**

- `retrodiction-harness/scripts/evaluate.py` - render template (`{{var}}` substring replace, no Jinja), call Claude via `urllib`, parse JSON with regex fallback, score
- `retrodiction-harness/scripts/iterate.py` - primary metric `trifecta_roi_pct`, tiebreaker `top3_all_accuracy`; LLM-driven `mutate_prompt()` proposes one strategic change per iteration
- `retrodiction-harness/scripts/scoring.py` - 7 metrics including simulated trifecta ROI from actual exotic payoffs
- `ai-derby/predictions.py` - bridges race-day `picks.json` shape into the harness's `render_prompt()`; writes through to Supabase via `persist_prediction()`
- `ai-derby/scripts/db.py` - Supabase upserts; partial-update semantics (only passed fields written)

## 5 · External integrations detected

(Found via grep across `ai-derby/`, `analysis/`, `retrodiction-harness/`.)

- **Anthropic Claude** - `retrodiction-harness/scripts/evaluate.py:call_llm()` via raw `urllib`. No SDK.
- **Supabase** - `ai-derby/scripts/db.py`, plus 14 modules importing from it (build_racecard, commentary, predictions, server, all operator scripts)
- **TheRacingAPI** - `ai-derby/build_racecard.py` pulls entries/scratches
- **ElevenLabs** - voice agent surface, referenced in CLAUDE.md (live demo flow)
- **Lovable** - frontend dashboard generator (`rob-api/dashboard/`)

## 6 · Tests / CI

- **Test directory:** `retrodiction-harness/tests/` (6 files, pytest) + `ai-derby/test_*.py` (6 files, pytest)
- **Test framework:** `pytest>=7.4.0`
- **CI config:** none detected at root (no `.github/workflows/` confirmed)
- **Coverage signal:** none detected

## 7 · Git history shape

- **First commit:** 2026-04-01
- **Last commit:** 2026-06-06
- **Total commits:** 141 (150 across all branches per `git shortlog`)
- **Calendar months of active work:** ~2.3 months (April through early June 2026)
- **Top 3 busiest months:**
  - 2026-05 - 65 commits (Derby/Oaks weekend + Pimlico build-out)
  - 2026-04 - 53 commits (initial stack + Derby weekend prep)
  - 2026-06 - 23 commits (Saratoga meet, ongoing)
- **Contributors:** Solo - JaySmith502 (150 commits)

## 8 · Size signals

(Counts include some vendored/cached directories in `rob-api/.uv-cache/`; the *first-party* surface is roughly Python in `ai-derby/`, `retrodiction-harness/`, `analysis/`, `rob-api/src/`, and `archive/`.)

| Language | Files (incl. cache) | Lines (incl. cache) |
|---|---:|---:|
| Python | 5,064 | 96,032 |
| TypeScript | 4,363 | 47,577 |
| TSX | 171 | 14,159 |
| JavaScript | 10,948 | 35,030 |
| Markdown | 770 | 17,564 |

**First-party surface (estimate, excluding `rob-api/.uv-cache/` and `node_modules`):** mostly the Python in `ai-derby/`, `retrodiction-harness/`, `analysis/` plus the Markdown docs/handoffs corpus. The graphify report says **156 files · ~488,586 words** in the documented surface.

## 9 · Honest gaps

What the repo did **not** tell us - these are things resolved by the operator's answers:

- Whether broadcaster/operator names (Don, Erica) are publishable - answered: yes, name everyone
- Outcome metrics beyond the one-pager - answered: lead with +10.9% trifecta-box ROI / 43 races
- The opinionated thesis - answered: retrodiction beats vibes (prompts measurably improve when graded against past races)
- Variant + colorway - answered: Variant B (Thesis-as-hero) + Blue
- Anything proprietary about TheRacingAPI contract / cost / quotas - not in repo
- ROI confidence intervals beyond the headline - see `analysis/out/ONE_PAGER.md` (referenced in memory, not opened here)

If you change your mind on any of those later, edit the draft directly - the skill won't re-run questions.

## 10 · Notebooks / artifacts skipped intentionally

- 0 `.ipynb` files detected
- 0 schema-migration files at the SQL level (Supabase schema lives in `ai-derby/project_aiderby_supabase_architecture.md` as narrative)
- PDFs in `ai-derby/pdfs/` and `archive/2026-derby-weekend/picks/` - printed-artifact provenance, not summarized
- `aiderby-946dd8e5/` looks like a worktree/checkpoint - not inspected
- `rob-api/.uv-cache/` - vendored Python deps, ignored

---

*End of sidecar. The case-study draft lives at `race-o-bot.md` in this same folder.*
