# Research sidecar - data-guardian

Generated by `case-study-from-repo` on 2026-06-07 from `C:\Users\smith\Documents\2 Projects\JH_Outdoors`.

**Use this file as your fact-check reference when editing `data-guardian.md`.** Every claim in the draft should be traceable back to a line here, or to one of the questions you answered during generation. If something can't be traced, treat it as a placeholder to verify before publishing.

---

## 1 · Repo identity

- **Path:** `C:\Users\smith\Documents\2 Projects\JH_Outdoors`
- **Slug derived:** `data-guardian`
- **Default branch:** `main`
- **Remote:** `https://github.com/JaySmith502/JH_Outdoors.git` (private - do not link publicly without owner consent)

## 2 · README excerpt

> (No `README.md` at repo root.) Project identity is documented in `CLAUDE.md`, which describes Data Guardian as: a production Flask app the operator (Brett) uses to scan vendors, review agent-found images, and approve uploads back to Shopify. Most-used flow is **Pull & Scan** (vendor scan + auto-correct + missing-image queue) followed by image and description review.

Other root-level prose: `MEMORY.md`, `seo-desc-project.txt`, `running_instructions.txt`, `Scoping/` directory, `docs/specs/` and `docs/plans/`.

## 3 · Stack manifest

**Manifest file detected:** `pyproject.toml`

- **Language:** Python 3.11+ (3.12 in production per CLAUDE.md)
- **Framework:** Flask 3.0
- **Key runtime deps:**
  - `flask>=3.0`, `flask-login>=0.6`, `gunicorn>=22.0`
  - `anthropic>=0.40` - Claude Vision for image verification, Haiku 4.5 for SEO description generation
  - `requests>=2.31` - Shopify GraphQL Admin API client
  - `apscheduler>=3.10` - background job scheduling
  - `firecrawl-py>=1.0`, `google-search-results>=2.4` (SerpAPI), `exa-py>=1.0` - three-tier image search
  - `python-dotenv>=1.0`
- **Dev deps:** `pytest>=8.0`, `pytest-cov>=4.0`
- **Persistence:** SQLite on disk (`data/brain.db`) - no managed DB. Auto-seeded from `data/seed/*.json` on container start.
- **Deploy:** Docker on Hetzner (per CLAUDE.md), `Dockerfile` + `docker-entrypoint.sh` at repo root.

## 4 · Architecture sketch

**Top-level folders:**

```
src/guardian/        Core library (importable from app/ and scripts/)
  brain.py           BrainDB - SQLite read/write for The Brain lookup tables (611 LOC)
  schema_v1.py       v1 tables: change_log, image_queue, unknown_data_queue
  normalizer.py      SKU parsing, title rules, RawProduct adapters (554 LOC)
  shopify_client.py  GraphQL pulls, variant updates, media upload (507 LOC)
  shopify_adapter.py Shopify product → RawProduct mapping
  comparison.py      Diff RawProduct vs. canonical, emit corrections
  image_detection.py find_missing_color_images() - per-color gap finder
  image_search.py    3-tier image search: site-scoped → open Google → Exa (189 LOC)
  image_verify.py    Claude Vision verification of candidates (208 LOC)
  image_agent.py     Orchestrator: search → verify → download
  description_detection.py  is_description_empty() - blank/tag-only HTML check
  description_agent.py      generate_description() - Haiku 4.5 SEO copy

app/                 Flask dashboard
  routes/
    vendors.py       Vendor list, scan trigger, missing-image scan trigger (271 LOC)
    images.py        Image queue review pending/review/manual (460 LOC)
    agent_runner.py  Run image agent against selected pending rows
    brain_editor.py  CRUD on color_map, type_map, tag_rules
    changelog.py     Audit trail of pushed corrections + undo (267 LOC)
    descriptions.py  Empty-description scan, AI generation, review, push (342 LOC)
    unknowns.py      Q&A queue for ambiguous data
  templates/, static/, auth.py, polling.py, cleanup.py

data/
  brain.db           SQLite Brain
  brain.db.bak-*     Timestamped backups before risky writes
  seed/              JSON/CSV inputs (vendor_map, color_codes_*, etc.)
  images/            Local copies of agent-downloaded candidates
```

**Spine files inspected:**

- `src/guardian/brain.py` (611 LOC) - central SQLite read/write layer. The "Brain" holds `vendor_map`, `sku_parse_rules`, `color_map`, `type_map`, `tag_rules`, `title_rules` plus the v1 operational queues.
- `src/guardian/normalizer.py` (554 LOC) - SKU parsing (`{JH_PREFIX}-{MFG_CODE}|{COLOR_CODE}|{SIZE}`), title rules, RawProduct adapters. Vendor-specific parse rules live in the Brain.
- `src/guardian/shopify_client.py` (507 LOC) - Admin GraphQL client: product pulls, variant updates, media upload.
- `app/routes/images.py` (460 LOC) - operator review UI for the image queue (pending → review → manual states).
- `app/routes/descriptions.py` (342 LOC) - empty-description scan, AI generation, side-by-side review, push to Shopify.
- `app/routes/changelog.py` (267 LOC) - audit trail of every pushed correction, with conflict-classified undo (Chunk 6 smoke test still pending per CLAUDE.md).

## 5 · External integrations detected

(Confirmed from `pyproject.toml` dependencies and CLAUDE.md.)

- **Shopify Admin API (GraphQL)** - primary system of record; `shopify_client.py` handles product/variant/media operations.
- **Anthropic Claude** - Claude Vision verifies image candidates before queueing (`image_verify.py`); Claude Haiku 4.5 drafts prose-only SEO descriptions (`description_agent.py`).
- **SerpAPI** (`google-search-results`) - open-Google tier of the image search cascade.
- **Exa** (`exa-py`) - third tier of the image search cascade.
- **Firecrawl** (`firecrawl-py`) - declared as a dep; site-scoped scraping for Tier 1 of the image search.

## 6 · Tests / CI

- **Test directory:** `tests/` - 23 test files
- **Test framework:** pytest (`pyproject.toml` configures `testpaths = ["tests"]`, `--import-mode=importlib`)
- **Test surface:** every core module has a paired test file - `test_brain.py`, `test_brain_writes.py`, `test_changelog_undo.py`, `test_changelog_undo_schema.py`, `test_comparison.py`, `test_description_agent.py`, `test_description_detection.py`, `test_description_routes.py`, `test_description_schema.py`, `test_health.py`, `test_image_agent.py`, `test_image_cleanup.py`, `test_image_detection.py`, `test_image_search.py`, `test_image_verify.py`, `test_normalizer.py`, `test_polling.py`, `test_routes.py`, `test_shopify_adapter.py`, `test_shopify_client.py`, `test_auth.py`, `test_changelog_prune.py`.
- **Stated test count:** CLAUDE.md says "303 passing" for the description-generation feature.
- **CI config:** none detected at repo root (no `.github/workflows/` visible in top-level listing).

## 7 · Git history shape

- **First commit:** 2026-03-03
- **Last commit:** 2026-06-04
- **Total commits:** 185
- **Span:** ~13 weeks of active work (Mar–Jun 2026)
- **Busiest months:**
  - 2026-03 - 93 commits
  - 2026-04 - 49 commits
  - 2026-05 - 38 commits
  - 2026-06 - 5 commits
- **Contributors:** Solo (`JaySmith502` 184 commits, `J Smith` 1 commit - same author).

## 8 · Size signals

| Language | Files | Lines |
|---|---:|---:|
| Python (all) | 64 | ~12,964 |
| Python (`src/guardian/` core) | 12 | 2,682 |
| Python (`app/routes/` Flask) | 8 | 1,636 |
| Python (`tests/`) | 23 | (not counted separately) |

**Total tracked Python:** 64 files, ~13K lines (excluding `.venv`, `.git`, `.worktrees`, `.pytest_cache`, `.pytest-tmp`).

## 9 · Operational scope (from CLAUDE.md, not code-extractable)

- **Source catalog:** `JH Full Product Catalog 031026 - products_export_1.csv` - ~83K rows, ~7K products, 253 vendors.
- **Operator:** Single human (Brett) reviews all agent output before push.
- **SKU format:** `{JH_PREFIX}-{MFG_PRODUCT_CODE}|{COLOR_CODE}|{SIZE}` - vendor-specific, rules stored in The Brain.
- **Shopify Option Value limit:** 15 chars - drives the `color_map.color_name` expansion column.

## 10 · Honest gaps

What the repo did **not** tell us - these were resolved via the 5 questions:

- Client identity → user requested anonymization ("a Midwest outdoors distributor with an e-commerce shop"). **Strip any reference to J&H Outdoors, Brett, or the GitHub username from the draft.**
- Real-world outcome metrics (time saved, error rate before/after) - not derivable from repo.
- Public links / press / launch date - none.
- Thesis - user-confirmed: "Small retailers can run enterprise-grade catalog hygiene by putting humans in the loop on AI agents, not by replacing them."
- Variant: A (Story arc), color: Red.
- Headline metric: "~7,000 products / 253 vendors governed by 1 operator" - both numbers come from CLAUDE.md and are derivable from the catalog CSV; safe to use.

## 11 · Artifacts skipped intentionally

- 0 `.ipynb` files
- 1 SQLite schema file (`schema_v1.py`) - narrated above, not duplicated
- `data/seed/*.json`, `data/images/*` static assets - not summarized
- `.planning/`, `.worktrees/`, `docs/superpowers/`, `docs/initial scoping/` - workflow scaffolding, not product
- Two large XLSX/CSV files at repo root - source data, not product code

---

*End of sidecar. The case-study draft lives at `data-guardian.md` in this same folder.*
