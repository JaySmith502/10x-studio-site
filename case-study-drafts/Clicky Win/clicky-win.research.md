# Research sidecar — clicky-win

Generated by `case-study-from-repo` on 2026-06-07 from `C:\Users\smith\Documents\2 Projects\clicky-win`.

**Use this file as your fact-check reference when editing `clicky-win.md`.** Every claim in the draft should be traceable back to a line here, or to one of the questions you answered during generation. If something can't be traced, treat it as a placeholder to verify before publishing.

---

## 1 · Repo identity

- **Path:** `C:\Users\smith\Documents\2 Projects\clicky-win`
- **Slug derived:** `clicky-win`
- **Default branch:** `main`
- **Remote (if set):** `https://github.com/JaySmith502/clicky-win.git`

## 2 · README excerpt

> Hi, this is Clicky. It's an AI teacher that lives as a buddy next to your cursor. It can see your screen, talk to you, and even point at stuff. Kinda like having a real teacher next to you.
>
> This is the open-source version of Clicky for those that want to hack on it, build their own features, or just see how it works under the hood.

Note: the root README is the upstream Farza `clicky` (macOS) README, vendored at fork time. The Windows port lives under `clicky-py/` and is documented in `docs/PRD.md` (titled **ClickyWin — Product Requirements Document**, status "Draft v1", dated 2026-04-09, owner `@JaySmith502`).

Full README at `README.md`. Full PRD at `docs/PRD.md`.

## 3 · Stack manifest

**Manifest file detected:** `clicky-py/pyproject.toml` (primary, active port). Secondary: `worker/package.json` (shared Cloudflare Worker), `leanring-buddy.xcodeproj` (vendored upstream Swift source).

- **Language(s):** Python 3.12 (active port), Swift (reference / upstream), TypeScript (Worker proxy)
- **Framework(s):** PySide6 (Qt6) for the Python desktop UI; Cloudflare Workers for the proxy; SwiftUI + AppKit in the reference Mac app
- **Key runtime deps (clicky-py):**
  - `pyside6>=6.7` — Qt6 desktop UI, tray, frameless panel
  - `sounddevice>=0.4.6` + `numpy>=1.26` — PCM16 mic capture and waveform buffer
  - `mss>=9.0` — multi-monitor screenshot capture
  - `pynput>=1.7.6` — system-wide modifier-only push-to-talk hotkey + click-outside dismissal
  - `httpx>=0.27` — Worker proxy HTTP client (Claude SSE, ElevenLabs TTS)
  - `websockets>=12.0` — AssemblyAI v3 realtime transcription socket
  - `pillow>=10.0` — screenshot encoding
  - `platformdirs>=4.0` — cross-platform config path resolution
- **Key dev/build deps:** `pytest`, `ruff`, `pyinstaller>=6.0` (onedir distribution target)
- **Scripts of interest:**
  - `clicky = "clicky.__main__:main"` (`pyproject.toml` `[project.scripts]`)
  - `scripts/release.sh` (legacy from upstream Mac build pipeline)

## 4 · Architecture sketch

**Top-level folders (one level deep):**

```
clicky-win/
  clicky-py/             # Active Windows port (Python 3.12 + PySide6)
    clicky/
      __main__.py        # Entry point — bootstraps QApplication + config
      app.py             # Orchestration / state machine
      config.py          # TOML config loader, platformdirs-resolved path
      hotkey.py          # Strict modifier-only Ctrl+Alt monitor (pynput)
      icon_factory.py    # Programmatic tray icon w/ state colors
      mic_capture.py     # sounddevice PCM16 streaming → Qt signal
      state.py           # VoiceState enum (idle/listening/processing/responding)
      ui/                # Frameless dark panel, waveform view, tray
    tests/
      test_config.py
    pyproject.toml
    uv.lock
  leanring-buddy/        # Vendored upstream Swift source (reference only — typo intentional)
  leanring-buddy.xcodeproj/
  leanring-buddyTests/
  leanring-buddyUITests/
  worker/                # Cloudflare Worker proxy (shared with upstream)
    src/index.ts         # /chat, /tts, /transcribe-token (141 lines)
  docs/
    PRD.md               # ClickyWin v1 PRD — the canonical product spec
    superpowers/
  scripts/
    release.sh
  AGENTS.md              # Single source of truth for AI coding agents
  README.md              # Upstream Farza README (Mac)
```

**Spine files inspected:**

- `clicky-py/clicky/__main__.py` — entry point: bootstraps `QApplication`, loads/creates config, launches tray + panel
- `clicky-py/clicky/app.py` — orchestration layer wiring tray → panel → hotkey → mic capture → (planned) LLM + TTS
- `clicky-py/clicky/hotkey.py` — strict modifier-only Ctrl+Alt detector via pynput, with `keyDown`/`keyUp` Qt signals
- `clicky-py/clicky/mic_capture.py` — sounddevice PCM16 streaming, chunks marshalled to UI via `QByteArray` on the Qt signal bus
- `clicky-py/clicky/ui/` — frameless dark `QWidget` panel anchored to tray icon, custom 60fps waveform view
- `worker/src/index.ts` — Cloudflare Worker proxying Anthropic (SSE), ElevenLabs TTS, and AssemblyAI token issuance
- `docs/PRD.md` — product spec: v1 scope (tray + push-to-talk + screenshot + Claude + TTS, **no flying cursor overlay**)
- `AGENTS.md` — architecture doc inherited and extended from upstream; describes the Mac app reference behavior

## 5 · External integrations detected

- **Anthropic Claude** — proxied via `worker/src/index.ts` `/chat` route. Models: Sonnet 4.6 default, Opus 4.6 optional (per PRD §"Claude conversation").
- **AssemblyAI** — realtime streaming transcription (`u3-rt-pro` model). Worker `/transcribe-token` issues short-lived (~480s) websocket tokens; client opens v3 websocket directly.
- **ElevenLabs** — TTS via Worker `/tts` route, `eleven_flash_v2_5` model.
- **Cloudflare Workers** — proxy that holds all three secrets so the desktop binary ships no keys.
- **PostHog** — analytics (Mac reference only; not yet wired into the Python port).
- **PySide6 / Qt6** — desktop framework (LGPL).
- **sounddevice + PortAudio** — mic capture backend on Windows.
- **mss** — multi-monitor screen capture (planned use; PRD §"Screen awareness").

## 6 · Tests / CI

- **Test directory:** `clicky-py/tests/` (`test_config.py` only at time of writing)
- **Test framework:** pytest
- **CI config:** none detected
- **Coverage signal:** none

## 7 · Git history shape

- **First commit (this branch):** 2026-04-07
- **Last commit:** 2026-04-09
- **Total commits on `main`:** 36
- **Calendar months of active work:** 1 (2026-04)
- **Top busiest month:** 2026-04 — 36 commits
- **Contributors (across all branches, includes upstream):**
  - JaySmith502 — 97 commits (the ClickyWin Windows port author)
  - farzaa — 20 commits (upstream Mac app author)
  - farza — 10 commits (upstream)

Read: this repo is a fork of `farzaa/clicky`. The Windows port (`clicky-py/`) is solo work by JaySmith502 over a focused April 2026 sprint; the Mac source is preserved as a reference but is not being modified.

## 8 · Size signals

| Language | Files | Lines |
|---|---:|---:|
| Swift (vendored Mac reference) | 25 | 7,751 |
| Python (active Windows port) | 14 | 1,258 |
| TypeScript (Cloudflare Worker) | 1 | 141 |

**Total tracked:** ~40 source files, ~9,150 lines across Python + Swift + TS (excluding `node_modules`, `.git`, `dist`, `build`, virtualenvs, `uv.lock`).

## 9 · Honest gaps

What the repo did **not** tell us — these are the things you (the user) answered during generation, or are still placeholders:

- Whether this is client work, self-built, or a contribution (the repo looks self-built — solo author JaySmith502, public OSS, MIT-licensed)
- Whether to name the upstream author (Farza / @FarzaTV) explicitly or anonymize
- Real-world outcome metrics — none extractable; PRD describes target experience but project is **pre-v1** (mic capture + waveform just landed at Phase 3.1; LLM, screenshot, TTS not yet wired)
- Public links / press / event dates
- The opinionated thesis the case study should prove
- Variant + colorway choice

If you change your mind on any of those later, edit the draft directly — the skill won't re-run questions.

## 10 · Notebooks / artifacts skipped intentionally

- 0 `.ipynb` files
- 0 migration files
- Static assets present but not summarized: `clicky-demo.gif` (2.4 MB upstream hero), `dmg-background.png`, `appcast.xml`, `Clicky_intro_video.txt`

## 11 · Status note (extra, not in template)

Per the most recent commits the Windows port is at **Phase 3.1 complete** — tray, panel, hotkey, mic capture, and live waveform are wired. The Claude streaming client, screenshot capture, ElevenLabs TTS playback, and panel response UI are scoped in the PRD but not yet implemented in `clicky-py/`. Any case-study claim about "shipped" behavior should be checked against the PRD scope vs. the actual `clicky-py/clicky/` module list above.

---

*End of sidecar. The case-study draft lives at `clicky-win.md` in this same folder.*
