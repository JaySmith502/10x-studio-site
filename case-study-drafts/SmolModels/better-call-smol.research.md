# Research sidecar — better-call-smol

Generated by `case-study-from-repo` on 2026-06-07 from `C:/Users/smith/Documents/2 Projects/SmolModels`.

**Use this file as your fact-check reference when editing `better-call-smol.md`.** Every claim in the draft should be traceable back to a line here, or to one of the 5 questions you answered during generation. If something can't be traced, treat it as a placeholder to verify before publishing.

---

## 1 · Repo identity

- **Path:** `C:/Users/smith/Documents/2 Projects/SmolModels`
- **Slug derived:** `better-call-smol` (from project tagline; repo directory is `SmolModels`)
- **Default branch:** `main`
- **Remote (if set):** not inspected
- **Internal project name:** `slm-advisor` (per `pyproject.toml`)
- **Marketing name:** "Better Call Smol"

## 2 · README excerpt

> # Better Call Smol
>
> Small, purpose-built AI advisor that recommends SLM / Hybrid / API / Just-prompt / No-AI for a use case.
>
> - Spec: `docs/superpowers/specs/2026-05-08-slm-advisor-design.md`
> - Plan: `docs/superpowers/plans/2026-05-08-slm-advisor.md`

Supplemental purpose framing (from `idea.txt`, author's own words):

> "Could I train a small model that helps people determine how to break down their process into either small, large (api) or hybrid model needs? … feels like it wouldn't land right if I had to use ChatGPT or Claude to answer the question."

So the project is explicitly a self-proving demo: an SLM that recommends SLMs.

## 3 · Stack manifest

**Manifest files detected:** `pyproject.toml` (training pipeline), `web/package.json` (Next.js frontend), `Workflow Advisor/package.json` (separate Lovable/Vite app).

- **Languages:** Python 3.11 (training pipeline + dataset), TypeScript (Next.js frontend, Vite advisor app).
- **Frameworks:** Oumi (LoRA SFT trainer), Hugging Face datasets/hub, Next.js (web demo), Vite + bun (Workflow Advisor sub-app), Tailwind/shadcn-style components in web.
- **Key runtime deps (Python):**
  - `anthropic>=0.40` — used to *generate* the training dataset (not at inference)
  - `datasets>=3.0`, `huggingface-hub>=0.27` — dataset publishing to HF
  - `pydantic>=2.9`, `python-dotenv`, `tqdm`
- **Key dev/build deps:** pytest, pytest-mock, ruff (line-length 100, Py3.11 target).
- **Scripts of interest:**
  - `scripts/augment_dataset.py` — synthetic-augments seed examples via Claude Sonnet 4.6 into the LoRA training set
  - `scripts/hf_push.py` — pushes the dataset to the Hugging Face hub
- **Oumi configs (`oumi/`):** `train.yaml` (LoRA SFT on `meta-llama/Llama-3.2-3B-Instruct`, r=16, α=32, dropout 0.05, 3 epochs, lr 2e-4, bf16, all attention + MLP target modules); `eval.yaml`; `judge.yaml` (LLM-as-judge rubric).

## 4 · Architecture sketch

**Top-level folders:**

```
.
├── DESIGN.md                  # (note: holds a Superthread-brand style reference, possibly mis-named)
├── README.md
├── conftest.py
├── data/
│   ├── seeds/slm-advisor-seeds.jsonl
│   ├── train_v2.jsonl         # 786 lines
│   └── test_v2.jsonl          # 93 lines
├── docs/
│   ├── HANDOFF.md             # session handoff notes
│   ├── roadmap.md             # v1 → v4 plus cross-version backlog
│   └── superpowers/           # specs + plans
├── idea.txt                   # origin-story brain-dump
├── netlify.toml               # web frontend deploy
├── next_chat.txt              # session resume note
├── oumi/{train,eval,judge}.yaml
├── pyproject.toml
├── scripts/
│   ├── augment_dataset.py     # 12.8 KB
│   └── hf_push.py
├── slm-advisor.jsonl          # 19 anchor examples (top-of-funnel seeds)
├── src/dataset/               # generation pipeline
│   ├── cli.py
│   ├── dedupe.py
│   ├── generation.py
│   ├── math_check.py          # validates kWh / $ math in generated answers
│   ├── schema.py              # 5-line response schema validator
│   └── seeds.py
├── tests/dataset/             # pytest suite + bad/good fixtures
├── uv.lock
├── web/                       # Next.js demo
│   ├── app/{about,api,chat}/, layout.tsx, page.tsx
│   ├── components/{ChatBox,LeadCaptureForm,LolaAvatar,ResponseCard}.tsx
│   ├── lib/{footprintMath,oumi,parseResponse,types,utils}.ts
│   └── __tests__/             # vitest
└── Workflow Advisor/          # second front-end (Lovable-generated Vite app, bun, wrangler)
```

**Spine files inspected (top ~50 lines each):**

- `src/dataset/schema.py` — enforces the 5-line output schema (`**Recommendation** … Why: … Cost: … Footprint: … Next:`); allowed recommendation set: `SLM-only | Hybrid | API-only | Just-prompt-for-now | Automation | No-AI`.
- `src/dataset/generation.py` — Claude-API-driven synthetic example generation. Defaults to `claude-sonnet-4-6`. System prompt explicitly forbids product names and forces *category-level* recommendations ("a 3B-class small model", not "Phi-3 mini").
- `src/dataset/math_check.py` — checks that generated footprint numbers (W, kWh, $/mo) are internally consistent — a guardrail against the seed model hallucinating math.
- `oumi/train.yaml` — LoRA r=16 SFT on Llama-3.2-3B-Instruct, dataset `JaySmith502/slm-advisor` (private HF repo).
- `web/lib/oumi.ts` — client for the Oumi-hosted inference endpoint that serves the fine-tuned model to the Next.js frontend.
- `web/components/ChatBox.tsx`, `ResponseCard.tsx` — chat UI rendering the structured 5-line response.
- `web/components/LolaAvatar.tsx` — "Lola" mascot (deleted `lola_standing.png` shows in git status; recent commit message: "Added social preview and removed next filler svgs").

## 5 · External integrations detected

(Found via dependency manifest + grep.)

- **Anthropic API (Claude Sonnet 4.6)** — used *only for training-data generation*, not at user inference. Imported in `src/dataset/generation.py`; env var `ANTHROPIC_API_KEY`.
- **Hugging Face Hub** — dataset hosting (`JaySmith502/slm-advisor`, private) and base-model pull (`meta-llama/Llama-3.2-3B-Instruct`). Env: `HF_TOKEN`, `HF_DATASET_REPO`.
- **Oumi** — used both as the LoRA SFT trainer (`oumi train -c oumi/train.yaml`) and as the hosted inference endpoint that serves the fine-tuned model to the website (env: `OUMI_API_KEY`, `OUMI_ENDPOINT`).
- **Formspree** — lead-capture form submission from the web UI (env: `FORMSPREE_ENDPOINT`).
- **Netlify** — web frontend deploy (`netlify.toml`).
- **Lovable** — `.lovable/` directory inside `Workflow Advisor/` indicates that sub-app was scaffolded with Lovable.
- **Cloudflare Workers** — `wrangler.jsonc` present in `Workflow Advisor/` (deploy target for the Vite app).

## 6 · Tests / CI

- **Test directories:** `tests/dataset/` (pytest, with good + intentionally-bad JSONL fixtures); `web/__tests__/` (vitest).
- **Test framework:** pytest (Python) + vitest (TS).
- **CI config:** none detected (no `.github/workflows/`, no `.gitlab-ci.yml`).
- **Coverage signal:** none.

## 7 · Git history shape

- **First commit:** 2026-05-08
- **Last commit:** 2026-05-10 (most recent in `git log`; file mtimes show edits through May 14)
- **Total commits:** 33–34 (single contributor)
- **Calendar months of active work:** 1 (May 2026)
- **Top busiest month:**
  - 2026-05 — 33 commits
- **Contributors:** JaySmith502 (solo)

Recent commit subjects (most recent first): *"Added social preview and removed next filler svgs," "edited button for less vertical padding," "Updated footer," "Changed formspree form to email contact," "Added web frontend to project and netlify deploy file."*

## 8 · Size signals

| Language | Files | Lines (rough) |
|---|---:|---:|
| Python + TypeScript (combined, non-vendored) | — | **~2,685 total** |
| Training data | 2 JSONL | 786 train + 93 test = **879 examples** |
| Seeds | 1 JSONL | 19 anchor examples |

**Total tracked source:** ~2.7K lines of code across Python pipeline + Next.js app + Lovable Vite app (excluding `node_modules`, `.git`, `dist`, `.next`, virtualenvs, `egg-info`).

## 9 · Honest gaps

What the repo did **not** tell us — these are the things you (the user) had to answer during generation:

- Whether to frame this as 10x-Studio-for-client or self-built founder project
- Real-world deployment outcome metrics (model rubric scores, inference cost, demo usage)
- Public launch / talk date (roadmap mentions a "July 2026 talk demo" — confirm)
- The opinionated thesis the case study should foreground
- Variant + colorway choice

If you change your mind on any of those later, edit the draft directly — the skill won't re-run questions.

## 10 · Notebooks / artifacts skipped intentionally

- 0 `.ipynb` files
- 0 SQL/Prisma migrations
- Static assets in `web/public/` and `Workflow Advisor/public/` not enumerated (Lola mascot images, social preview, favicons)
- `uv.lock` (411 KB) and lockfiles in `web/`, `Workflow Advisor/` skipped — dependency *intent* read from manifests, not lock pinning

---

*End of sidecar. The case-study draft lives at `better-call-smol.md` in this same folder.*
