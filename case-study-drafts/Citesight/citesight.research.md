# Research sidecar — citesight

Generated by `case-study-from-repo` on 2026-06-07 from `C:\Users\smith\Documents\2 Projects\cite-sight-check`.

**Use this file as your fact-check reference when editing `citesight.md`.** Every claim in the draft should be traceable back to a line here, or to one of the 5 questions you answered during generation. If something can't be traced, treat it as a placeholder to verify before publishing.

---

## 1 · Repo identity

- **Path:** `C:\Users\smith\Documents\2 Projects\cite-sight-check`
- **Slug derived:** `citesight`
- **Default branch:** `main`
- **Remote (if set):** `https://github.com/JaySmith502/cite-sight-check.git`

## 2 · README excerpt

> Welcome to your Lovable project. Project URL: https://lovable.dev/projects/4b2db94b-974d-4b8e-9cfd-6884a085d981. Built with Vite, TypeScript, React, shadcn-ui, Tailwind CSS. Deployable via Lovable's Publish flow.

The README is the default Lovable scaffold and does not describe the product. The product brief lives in `citesight_prd.md`:

> **CiteSight** — A simple web application that validates legal citations using the CourtListener API and presents results in an intuitive traffic-light system. **Target users:** solo practitioners and attorneys at small/medium law firms (2–50 attorneys). **Pain points:** manual citation validation is time-consuming; enterprise legal AI tools cost $1000+/month; citation errors damage credibility. **Value proposition:** professional citation validation at $49/month — 95% cost savings vs. enterprise alternatives like Harvey.ai / CoCounsel.

(Full README is at `<repo>/README.md`. The PRD at `<repo>/citesight_prd.md` is the source of truth for product framing.)

## 3 · Stack manifest

**Manifest file detected:** `package.json`

- **Language(s):** TypeScript (98 files), TSX-dominant React
- **Framework(s):** React 18, Vite 5, Tailwind 3, shadcn/ui (Radix primitives), TanStack Query, React Router 6, React Hook Form + Zod, react-helmet-async
- **Key runtime deps:**
  - `@supabase/supabase-js` ^2.57 — auth, Postgres, Storage, Edge Functions client
  - `jspdf` ^3.0 — client-side PDF report generation
  - `dompurify` ^3.2 — sanitization for user-pasted brief text
- **Key dev/build deps:** `@vitejs/plugin-react-swc`, `typescript` 5.8, `eslint` 9 flat config, `lovable-tagger` (dev-mode component tagger from the Lovable build chain)
- **Scripts of interest:**
  - `npm run dev` — Vite dev server on `:8080`
  - `npm run build` / `build:dev` — production / dev-mode builds
  - `npm run lint` — eslint flat config; no test script wired up

## 4 · Architecture sketch

**Top-level folders:**

```
src/
  App.tsx                 # provider stack + routes
  pages/                  # Index-v2 (live), Index (legacy), Auth, NotFound
  components/             # feature components + shadcn ui/ + turnstile/
  hooks/                  # useAuth, useDashboard, use-mobile, use-toast
  services/               # citationExtractor, courtListener, database, pdfExport, pdfOcr
  utils/                  # citationParser, citationSentenceExtractor, pdfTextCleaner, logger
  integrations/supabase/  # client.ts + generated types.ts
supabase/
  functions/              # extract-citations, ocr-pdf, check-subscription,
                          # create-checkout, customer-portal, stripe-webhook  (Deno)
  migrations/             # 10 SQL migrations (RLS, storage, helper fns)
public/                   # favicon, sitemap.xml, robots.txt, site-preview.png
netlify.toml              # SPA redirect + strict CSP
```

**Spine files inspected (top ~50 lines each):**

- `src/App.tsx` — provider stack (Helmet, QueryClient, AuthProvider, Tooltip) + routes; `/` mounts `Index-v2`, `/test-original` keeps the legacy `Index` for A/B comparison.
- `src/services/courtListener.ts` — CourtListener v4 `citation-lookup` client, rate-limit state, traffic-light status normalization (`valid` / `warning` / `invalid` / `info`).
- `src/utils/citationSentenceExtractor.ts` — sentence-anchored extractor with explicit 50-char context padding, targeting "85–95% character reduction while maintaining 99%+ citation recall." Core IP of the pipeline.
- `src/services/pdfOcr.ts` + `supabase/functions/ocr-pdf/index.ts` — image-PDF fallback path; the edge function proxies to a `STERLING_URL` external OCR service.
- `supabase/functions/extract-citations/index.ts` — Deno edge function that re-runs citation extraction server-side for heavy payloads, auth-gated by Supabase JWT.
- `supabase/functions/stripe-webhook/index.ts` — Stripe events → `subscriptions` table writes that gate `useAuth.tsx` trial-vs-active state.

## 5 · External integrations detected

(Found via grep across the repo.)

- **CourtListener API v4** — `src/services/courtListener.ts` calls `citation-lookup`; whitelisted in `netlify.toml` CSP.
- **Supabase** — auth, Postgres (10 migrations), Storage (migration `003_storage_setup.sql`), Edge Functions. Client in `src/integrations/supabase/client.ts`.
- **Stripe** — checkout, customer portal, webhook (3 separate edge functions). Drives subscription gating in `useAuth.tsx` + `SubscriptionCard.tsx`.
- **Cloudflare Turnstile** — bot challenge on auth flow; `src/components/turnstile/` + whitelisted in CSP `script-src`.
- **Sterling (external OCR service)** — referenced via `STERLING_URL` env var inside `supabase/functions/ocr-pdf/index.ts`. Repo also contains `TROUBLESHOOTING_STERLING.md`.
- **Lovable** — `lovable-tagger` dev plugin in `vite.config.ts`; the repo is round-tripped to lovable.dev.
- **Netlify** — production deploy target via `netlify.toml`, including a strict CSP that hard-codes the allowed `connect-src` origins.
- **No LLM SDK detected** — no `openai`, `anthropic`, `langchain` imports. Validation is deterministic API lookup, not LLM-judged. This is deliberate per the PRD's "cost-effective alternative to enterprise LLM tools" positioning.

## 6 · Tests / CI

- **Test directory:** `src/utils/__tests__/citationSentenceExtractor.test.ts` (single file)
- **Test framework:** Jest-style `describe/it` syntax, **but no Jest or Vitest is installed**. Test is currently unrunnable; treat as documentation of intent.
- **CI config:** none detected (no `.github/workflows/`, no `.gitlab-ci.yml`)
- **Coverage signal:** none

## 7 · Git history shape

- **First commit:** 2025-09-04
- **Last commit:** 2025-10-10
- **Total commits:** 142
- **Calendar months of active work:** 2 (Sept–Oct 2025)
- **Top busiest months:**
  - 2025-09 — 110 commits
  - 2025-10 — 32 commits
- **Contributors:** Jay Smith (as `JaySmith502` 173, `Jay Smith` 27, `J Smith` 5 — same person across email configs) + `gpt-engineer-app[bot]` 32 commits (Lovable's automation account)

**Tempo signal:** ~5 weeks of dense work, predominantly solo, with Lovable assisting on the scaffold. The arc is short and recent — a viable "ship a vertical SaaS in 5 weeks" story if the user wants Variant A.

## 8 · Size signals

| Language | Files | Lines |
|---|---:|---:|
| TypeScript / TSX | 98 | 15,666 |
| SQL (migrations) | 10 | 488 |
| JS | 3 | — |
| CSS | 2 | — |

**Total tracked:** ~113 source files, ~16k lines (excluding `node_modules`, `.git`, `dist`).

## 9 · Honest gaps

What the repo did **not** tell us — these are the things you (the user) had to answer during generation:

- Whether this is a 10x Studio client build, your own product, or a collaboration
- Real subscriber counts, validation volume, or any production metric
- Whether the $49/month pricing is live and converting (PRD-stated, not confirmed)
- Public launch date or any press / event hooks
- The opinionated thesis the case study should prove
- Variant + colorway choice

If you change your mind on any of those later, edit the draft directly — the skill won't re-run questions.

## 10 · Notebooks / artifacts skipped intentionally

- 0 `.ipynb` files
- 10 SQL migration files (schema-only, not narrated in the case study body)
- 5 static assets in `public/` (not summarized)
- ~14 root-level `*.md` working notes (`SESSION_SUMMARY.md`, `VALIDATION_FIX_COMPLETE.md`, `TROUBLESHOOTING_STERLING.md`, `CORS_FIX_NEEDED.md`, etc.) — these are mid-build session logs, not living docs; useful for fact-checking the build narrative but not source-of-truth product framing.

---

*End of sidecar. The case-study draft lives at `citesight.md` in this same folder.*
