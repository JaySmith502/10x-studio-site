# Research sidecar — {{SLUG}}

Generated by `case-study-from-repo` on {{YYYY-MM-DD}} from `{{REPO_PATH}}`.

**Use this file as your fact-check reference when editing `{{SLUG}}.md`.** Every claim in the draft should be traceable back to a line here, or to one of the 5 questions you answered during generation. If something can't be traced, treat it as a placeholder to verify before publishing.

---

## 1 · Repo identity

- **Path:** `{{REPO_PATH}}`
- **Slug derived:** `{{SLUG}}`
- **Default branch:** `{{BRANCH}}`
- **Remote (if set):** `{{REMOTE_URL_OR_NONE}}`

## 2 · README excerpt

> {{README_FIRST_PARAGRAPHS_QUOTED}}

(Full README is at `{{REPO_PATH}}/README.md` if present.)

## 3 · Stack manifest

**Manifest file detected:** `{{MANIFEST_FILE}}`

- **Language(s):** {{LANGUAGES}}
- **Framework(s):** {{FRAMEWORKS}}
- **Key runtime deps:**
  - {{DEP_1}}
  - {{DEP_2}}
  - {{DEP_3}}
- **Key dev/build deps:** {{DEV_DEPS_SUMMARY}}
- **Scripts of interest:**
  - {{SCRIPT_1}}
  - {{SCRIPT_2}}

## 4 · Architecture sketch

**Top-level folders:**

```
{{TREE_LISTING_ONE_LEVEL_DEEP}}
```

**Spine files inspected (top ~50 lines each):**

- `{{SPINE_FILE_1}}` — {{ONE_LINE_PURPOSE}}
- `{{SPINE_FILE_2}}` — {{ONE_LINE_PURPOSE}}
- `{{SPINE_FILE_3}}` — {{ONE_LINE_PURPOSE}}
- `{{SPINE_FILE_4}}` — {{ONE_LINE_PURPOSE}}
- `{{SPINE_FILE_5}}` — {{ONE_LINE_PURPOSE}}

## 5 · External integrations detected

(Found via grep across the repo.)

- {{INTEGRATION_1}} — {{HOW_DETECTED, e.g. "imported in src/payments.ts"}}
- {{INTEGRATION_2}} — {{HOW_DETECTED}}
- {{INTEGRATION_3}} — {{HOW_DETECTED}}

If empty: *(none detected)*.

## 6 · Tests / CI

- **Test directory:** {{PATH_OR_NONE}}
- **Test framework:** {{e.g. pytest, vitest, jest, go test, none detected}}
- **CI config:** {{e.g. .github/workflows/ci.yml, .gitlab-ci.yml, none detected}}
- **Coverage signal:** {{e.g. codecov.yml, .coveragerc, none}}

## 7 · Git history shape

- **First commit:** {{FIRST_COMMIT_DATE}}
- **Last commit:** {{LAST_COMMIT_DATE}}
- **Total commits:** {{N}}
- **Calendar months of active work:** {{N}}
- **Top 3 busiest months:**
  - {{YYYY-MM}} — {{N}} commits
  - {{YYYY-MM}} — {{N}} commits
  - {{YYYY-MM}} — {{N}} commits
- **Contributors:** {{LIST_OR_SOLO}}

## 8 · Size signals

| Language | Files | Lines |
|---|---:|---:|
| {{LANG_1}} | {{N}} | {{N}} |
| {{LANG_2}} | {{N}} | {{N}} |
| {{LANG_3}} | {{N}} | {{N}} |

**Total tracked:** {{N}} files, {{N}} lines (excluding `node_modules`, `.git`, `dist`, `build`, virtualenvs).

## 9 · Honest gaps

What the repo did **not** tell us — these are the things you (the user) had to answer during generation:

- Client name / NDA constraints
- Real-world outcome metrics
- Public links / press / event dates
- The opinionated thesis the case study should prove
- Variant + colorway choice

If you change your mind on any of those later, edit the draft directly — the skill won't re-run questions.

## 10 · Notebooks / artifacts skipped intentionally

- {{NOTEBOOK_COUNT}} `.ipynb` files (noisy, skipped — open manually if relevant)
- {{MIGRATIONS_COUNT}} migration files (schema-only, not narrated)
- {{ASSET_COUNT}} static assets (not summarized)

---

*End of sidecar. The case-study draft lives at `{{SLUG}}.md` in this same folder.*
