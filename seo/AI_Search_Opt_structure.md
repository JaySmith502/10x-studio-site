````markdown
# Content Brief for AI-Era Search

**Ranks on Google + gets cited by AI Overviews**

## `{your keyword}`

---

## 1. The Answer Block

**What one question does this page answer?**

Write **100–150 words**:

- Direct answer
- No fluff
- No intro

**Goes at top of page**

AI Overviews pull from this block.

```text
{keyword}
→ {question}
→ {150w answer}
```
````

---

## 2. The Structure

**What are people also asking?**

Search `{keyword}` and find the PAA box.

Each question becomes one H2.

```text
{PAA Q1} → H2
{PAA Q2} → H2
{PAA Q3} → H2
{PAA Q4} → H2
```

AI reads sections independently.
Each must answer on its own.

---

## 3. The Edge

**What do the top 5 not do?**

Pick one:

- Original data
- Specific ICP
- Named framework
- Deeper subtopic

```text
"This page does {X} no one else has"
```

Can’t finish it?
Brief not ready.

---

## Brief Output

Hand this to Claude.

| Field         | Output              |
| ------------- | ------------------- |
| Keyword       | `{your keyword}`    |
| Core question | `{the one Q}`       |
| AEO block     | `{150-word answer}` |
| H2 map        | `{PAA questions}`   |
| Reader        | `{specific person}` |
| Edge          | `{the one thing}`   |
| CTA           | `{one action}`      |
| EEAT signal   | `{quote / data}`    |

---

## Pre-Publish Check

1. First **150-word answer**: core Q directly?
2. Every H2 maps to a PAA question?
3. Every section stands alone?
4. One cited data point per section?
5. Human gets something AI summary doesn’t?

```text
All YES → publish
Any NO → fix first
```

```

```
